import json

json_settings = json.dumps([
	{'type': 'numeric',
	'title': 'Startup Volume',
	'desc': 'Set the default startup volume for the picarputer',
	'key': 'startupvolume',
	'section': 'General'}])
